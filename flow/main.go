package flow

// var RegisteredFlows []Flow

func CreateNew() {}

func Run(collection string, operationType interface{}, data map[string]interface{}) {
	// for _, flow := range RegisteredFlows {
	// 	if flow.Trigger == operationType && flow.Collection == collection {
	// 		if len(flow.Steps) == 0 {
	// 			break
	// 		}

	// for _, step := range flow.Steps {
	// 	err := step.Handler(&data)
	// 	if err != nil {
	// 		fmt.Println(err)
	// 		break
	// 	}
	// }

	// fmt.Print("\n===================== Final ", collection, " Data =====================\n")
	// for k, v := range data {
	// 	fmt.Printf("%s: %v\n", k, v)
	// }
	// fmt.Print("======================================================\n\n")
	// }
	// }
}
